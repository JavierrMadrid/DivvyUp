<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Splash (Android 12+ y compat vía androidx.core:core-splashscreen) -->
    <style name="Theme.DivvyUp" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.DivvyUp.Main</item>
    </style>

    <!-- Tema principal de la app -->
    <style name="Theme.DivvyUp.Main" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/app_background_light</item>
    </style>
</resources>
